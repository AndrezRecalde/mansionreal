import { CalendarioReservas } from "../../calendario/section/CalendarioReservas";

/**
 * Componente que muestra directamente el calendario de reservas (HOSPEDAJE)
 * Ya no utiliza Tabs — se eliminó el tipo ESTADIA
 */
export const TabContent = () => {
    return <CalendarioReservas />;
};
